@model IEnumerable<ProyectoFinalVeterinaria.Models.Provincias>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descProvincia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descProvincia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProvincia }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProvincia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProvincia })
        </td>
    </tr>
}

</table>
