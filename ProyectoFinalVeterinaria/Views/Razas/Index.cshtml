@model IEnumerable<ProyectoFinalVeterinaria.Models.Razas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descRaza)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descRaza)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRaza }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRaza }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRaza })
        </td>
    </tr>
}

</table>
